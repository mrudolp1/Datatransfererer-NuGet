
BEGIN
	INSERT INTO tnx.tnx ([TNX FIELDS]) 
	OUTPUT INSERTED.ID INTO @Prev
	VALUES([TNX VALUES])
	SELECT @PrevID=ID FROM @Prev

	DECLARE @SubPrev TABLE(ID INT)
    DECLARE @SubPrevID INT

	--[MEMBERS]
	--[MATERIALS]
	--[BOLTS]

	[BASE]
	[UPPER]
	[GUYS]

	DELETE FROM @Prev
END